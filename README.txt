Run the following commands in the VS Code terminal
Make sure you have Node.js installed
---
npm install <- Do this to install all dependencies
compileAndRun.ps1 <- Run as a powershell script to compile and run the bot. If on linux, make it a bash script file first